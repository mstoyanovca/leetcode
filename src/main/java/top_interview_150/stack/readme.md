
#### Functions to remember

```
 Deque<String> deque = new ArrayDeque<>();
 String.join("/", deque);
 
 String[] input = s.replace(" ", "").split("(?<=\\+)|(?=\\+)|(?<=-)|(?=-)|(?<=\\()|(?=\\()|(?<=\\))|(?=\\))");
```
